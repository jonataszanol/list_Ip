<%= form_with(model: acesso, local: true) do |form| %>
  <% if acesso.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(acesso.errors.count, "error") %> prohibited this acesso from being saved:</h2>

      <ul>
      <% acesso.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :descricao %>
    <%= form.text_field :descricao %>
  </div>

  <div class="field">
    <%= form.label :obs %>
    <%= form.text_field :obs %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
